import { FilterCartPipe } from './filter-cart.pipe';

describe('FilterCartPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCartPipe();
    expect(pipe).toBeTruthy();
  });
});
